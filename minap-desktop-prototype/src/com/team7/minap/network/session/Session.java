/**
 * A session is created for the duration of each login. Singleton.
 */

package com.team7.minap.network.session;

import com.team7.minap.authentication.Authenticator;

public class Session {
	public static void signIn() {
		// Create an authenticator object
	}
	
	public static void signOut() {
		// Close network connection, return to sign on page
	}
}
